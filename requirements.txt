fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
httpx==0.28.0
aiofiles==24.1.0
Pillow==11.0.0
pytesseract==0.3.13
# PDF text extraction + page rendering for scanned pages (import as fitz)
PyMuPDF==1.24.14
pydantic==2.10.3
chromadb==0.5.15
# sentence-transformers==3.2.1  # Optional - enables vector search, install manually if needed

# PaddleOCR – only available on Python 3.10 / 3.11
paddlepaddle==2.6.2
paddleocr==2.9.1

# Windows credential manager for secure API key storage
keyring==24.3.1